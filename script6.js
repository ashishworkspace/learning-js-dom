const navItems = document.getElementsByClassName('nav-item');
console.log(navItems); // HTMLCollection

const navItemSelect = document.querySelectorAll('.nav-item');
console.log(navItemSelect); // NodeList